# 꿈의 탈출 — 개발 정리

> 작성일 2026-09-25 · 대상 빌드 `dream_escape.html` v0.1.8 · 개발 기간 2026-09-23 (1일)

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 게임 이름 | 꿈의 탈출 (Dream Escape) |
| 장르 | 웹 브라우저용 익스트랙션 어드벤처 |
| 한 줄 요약 | 등불 하나 메고 어두운 꿈에 들어가, 99초 안에 출구를 찾아 살아 나오면 주운 것이 내 것이 된다 |
| 형태 | HTML 파일 1개 (Canvas 2D, 외부 라이브러리·서버 없음) |
| 플랫폼 | PC·모바일 브라우저 (마우스 / 터치) |
| 저장 | 브라우저 localStorage (금화·룬·기록) |
| 개발 방식 | 대화형 개발 — 요청을 받아 즉시 구현하고 node 스텁 하네스로 검증, 총 8회 반복 |

## 2. 산출물과 링크

| 산출물 | 설명 |
|---|---|
| `dream_escape.html` | 게임 본체 (약 1,400줄, 모든 설정은 상단 `CFG`) |
| `index.html` | 짧은 주소용 리다이렉트 페이지 (게임 본체로 즉시 이동) |
| `PRD.md` | 제품 요구사항 정의서 |
| `GAME_DESIGN.md` | 게임기획서 |
| `DEV_SUMMARY.md` | 이 문서 |

- 저장소: https://github.com/duros221/dream-escape (public, `main`)
- 플레이: https://duros221.github.io/dream-escape/
- 로컬: `C:\Users\USER\claude_community\dream-escape`

## 3. 개발 경과

| 단계 | 요청 | 구현 | 검증 |
|---|---|---|---|
| v0.1.0 | 장르·규칙 원안 (등불, 90초, 몬스터 3~5마리, 조우 선택, 장비) | 커서 이동, 등불 확장, 카메라, 어둠 합성, 몬스터 AI(배회·인식·추격), 조우/전투/도망, 보물상자, 탈출구, HUD·미니맵 | 문법 검사 |
| v0.1.1 | 몬스터 외형 12종 + 해골·좀비·50m 거인 | `MONSTER_TYPES` 15종, 타입별 draw/eyes, 거인은 발만 렌더 | 15종 전부 배회·추격·전투·사망 상태로 240프레임 렌더 |
| v0.1.2 | 약함 65% / 같음 25% / 강함 10% | `CFG.TIERS` 등급 → 등급 안에서 공격·체력 비율 독립 선택 | 1,140마리 표본 67/23/10% |
| v0.1.3 | 속도 절반, 선택 중 타이머 진행, 등불 3m 시작 | `SPEED 3.5`, `MON_CHASE_MUL 0.5`, `choice` 상태에서 `tickTimer` | 선택 상태 1초 → 경과 1.00초 |
| v0.1.4 | 도망 시 체력 -2 | `FLEE_COST`, 0이 되면 실패 처리 | 50→48, 2→0 종료 확인 |
| v0.1.5 | box/map 용어, 몬스터 -20%, 자연 회복, 부활의 샘, 금화, 로비 저장 | `BOX`/`MAP_COLS`/`MAP_ROWS` 3×3, `MON_SPAWN_RATE 0.8`, 5초 +3 회복, box당 50% 샘, 금화 1~10, localStorage | 40판 평균 28.6마리(기대 28.8), 샘 4.5개, 회복·샘·금화·로비 합산/소실 확인 |
| v0.1.6 | 로비 화면, 룬 5종, 합치기/판매/구매 | 대기 애니메이션(숨쉬기·등불·먼지), 룬 카드 UI, `derivedStats`, 별도 공격속도 타이머 | 드롭률 20.8%(5,000회), 합치기·판매·구매 수치, 5초 전투 15타 vs 10타 |
| v0.1.7 | 제한 시간 99초 | `TIME 99`, 메시지 상수화 | 문법 검사 |
| v0.1.8 | 탈출구 목격 시 미니맵 표시, 남은 탈출시간 라벨, 전투 중 체력 바, 반대쪽 연타 도망 | `exitSeen` 플래그 + 맥동 표시, HUD 라벨·정수 초, `drawFoeBar`, `fleeTap` (반대 반원 3회/1.2초) | 목격 판정·유지, 방향 판정, 시간 창 만료, 도망 비용 적용 |
| 배포 | GitHub 업로드, Pages 404 해결 | 저장소 생성·푸시, Pages 활성화, `index.html` 리다이렉트 | 배포 URL HTTP 200, 제목 태그 확인 |

## 4. 최종 게임 규칙 요약

- **목표**: 99초 안에 탈출구에 도달. 체력 0 또는 시간 초과면 실패.
- **공간**: box 30m×30m, map = box 3×3 (90m×90m). 탈출구는 시작점에서 45m 이상 떨어진 랜덤 위치, 반경 5m 푸른 발광.
- **이동**: 커서(터치)를 따라 3.5m/s. 조작은 이동과 조우 선택(F/R)만.
- **빛**: 등불 3m 시작, 10초마다 +0.5m. 몬스터 인식 거리 = 등불 × 1.5.
- **주인공**: 체력 50, 공격 10, 0.5초당 1타, 5초마다 체력 +3. 룬 단위로 영구 성장.
- **몬스터**: box당 3~5 × 80%. 크기 0.5~5배(거인 50m 고정), 외형 15종. 등급 약함 65% / 같음 25% / 강함 10%, 공격·체력 비율 10~300%. 배회 0.5m/s, 추격은 항상 주인공보다 느림.
- **조우**: 접촉 시 [맞서 싸운다] / [도망친다]. 선택 중에도 시간 진행. 전투 중 체력 바 표시(공격력은 비공개). 도망은 R키 또는 반대쪽 3연타, 체력 -2.
- **보상**: 처치·보물상자(box당 1) → 금화 1~10 + 갑옷(+10 체력) 또는 무기(+5 공격) + 20% 룬. 부활의 샘(box당 50%)은 체력 100%, 1회용.
- **익스트랙션**: 탈출 성공 시 금화·룬을 로비에 합산, 실패 시 그 판의 것 전부 소실.
- **로비**: 금화·룬·능력치 표시. 같은 룬 3개 합치기 → 1단위 (공격 +1 / 5초당 공격 +1회 / 체력 +1 / 5초당 회복 +0.2 / 이동속도 +1, 기본 20). 룬 판매 5🪙, 100🪙로 랜덤 종류 3개 구매.

## 5. 기술 구조

- **단일 파일**: HTML + CSS + JS 하나. 외부 요청 없음 → 더블클릭·정적 호스팅 모두 동작.
- **상태 머신**: `title`(로비) → `play` → `choice` → `fight` → `play` … → `end`. 타이머는 `play`·`choice`·`fight`에서 진행.
- **좌표계**: 모든 길이는 미터. `S = 캔버스 한 변 / 30` 픽셀/m. 카메라는 주인공을 따라가며 map 경계에서 정지.
- **렌더 순서**: 바닥 장식(컬링) → 탈출구 → 샘 → 상자 → 개체(y 정렬) → 어둠 오버레이 → 전투 체력 바 → 떠오르는 글자 → HUD.
- **어둠 합성**: 오프스크린 캔버스에 어둠을 채우고 `destination-out`으로 등불·출구·떠도는 등불 구멍을 뚫은 뒤 본 캔버스에 덮는다. 그 위에 `lighter`로 주황 틴트, 인식한 몬스터의 눈은 어둠 위에 다시 그린다.
- **몬스터 외형**: `MONSTER_TYPES[]` — 각 타입은 `name, glow, draw(m,x,y,h,t,cam), eyes(m,x,y,h)` (+ `bounds`, `selfLight`, `giant` 옵션). 새 타입은 객체 하나 추가로 끝.
- **전투**: 주인공 타이머(`hitInterval = 5/(10+공격속도 단위)`)와 몬스터 타이머(0.5초)를 분리해 각자 `while` 루프로 처리.
- **저장**: `localStorage["dream-escape-lobby"] = {gold, escapes, runs, runes{5}, units{5}}`. 접근 실패 시 try/catch로 무시.
- **설정**: 밸런스는 `CFG`, 룬은 `RUNES`, 몬스터는 `MONSTER_TYPES`에 집중. 문서(`GAME_DESIGN.md` 14절)와 1:1 대응.

## 6. 검증 방법

브라우저 없이 개발했기 때문에 두 단계로 확인했다.

1. **문법 검사** — `<script>` 내용을 추출해 node의 `new Function(src)`로 파싱.
2. **런타임 스텁 하네스** — `document`, `window`, `localStorage`, Canvas 컨텍스트를 Proxy 스텁으로 대체해 node에서 게임을 실제로 돌린다. `startGame → update/draw 수백 프레임 → 상태 강제(인식·전투·도망·사망) → 수치 확인`. 매 반영마다 이 하네스로 "터지지 않음 + 수치가 기대와 일치"를 확인했다.

한계: 스텁은 **그림이 그려졌는가**만 보장하고 **어떻게 보이는가**는 보장하지 않는다. 몬스터 15종의 실제 외형, 로비 패널 배치는 사용자가 브라우저에서 확인하는 것으로 역할을 나눴다.

## 7. 배포

| 단계 | 내용 |
|---|---|
| 도구 | git 2.55, GitHub CLI 2.96 (계정 duros221) |
| 저장소 | `git init -b main` → 첫 커밋 → `gh repo create dream-escape --public --source=. --push` |
| Pages | `gh api -X POST repos/duros221/dream-escape/pages -f source[branch]=main -f source[path]=/` — 저장소 생성만으로는 켜지지 않아 처음 404가 났음 |
| 짧은 주소 | Pages는 `index.html`을 기본 문서로 쓰므로 `dream_escape.html`로 넘기는 리다이렉트 `index.html`을 추가 |
| 재배포 | 수정 후 `git add . ; git commit -m "..." ; git push` → 30초~1분 뒤 자동 반영 |

## 8. 설계 결정 및 해석

| 원문 / 상황 | 결정 | 이유 |
|---|---|---|
| "공격한다 vs 맞서 싸운다" | 맞서 싸운다 vs 도망친다 | 둘 다 싸움이면 선택이 아님 |
| "전체 지도는 화면의 10배" → "box 9개" | 3×3 | 후속 지시 반영 |
| 탈출구 가시성 | 반경 5m 자체 발광 (`EXIT_GLOW`, 0으로 끌 수 있음) | 등불만으로는 확률적으로 거의 불가능 — 수치 계산 후 결정 |
| 도망의 실효성 | 몬스터 1초 휘청, 항상 주인공보다 느림 | 도망이 선택지가 되게 |
| 룬 소유권 | 금화와 같이 탈출해야 확정 | 장르 규칙에 맞춤 |
| 부활의 샘 | 1회용 (`FOUNTAIN_REUSE`로 전환) | 무한 회복은 리스크 무력화 |
| 거인 | 크기 규칙 예외 50m, 발 기준 충돌 | 요청 반영 |
| 몬스터 체력 표시 | 조우 시 비공개, 전투 진입 후 표시 | "싸울지"는 도박, "계속 싸울지"는 판단 |
| 파일명 | `index.html` → `dream_escape.html`, 리다이렉트 `index.html` 별도 | 본체는 하나만 유지 |

## 9. 미결 사항 및 다음 단계

- 실플레이 데이터로 탈출 성공률 측정 → `EXIT_GLOW`, `SPEED`, `MON_SPAWN_RATE` 재조정
- 사운드(등불, 인식 경고, 거인 발소리, 타격음) 미구현
- 몬스터 15종 실제 외형과 로비 패널 배치의 시각 검수
- 이동속도 룬이 다른 룬보다 체감이 큼 — 룬 가치 균형 검토
- 로비 초기화 버튼 여부
- 확장 아이디어: 가짜 출구, 룬 시너지, 일일 시드, box 특성, 몬스터 도감
